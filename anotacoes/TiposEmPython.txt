os tipos de testos built-in são:

texto = str 

numericos =  int, float, complex

sequencia = list, tuple, range

mapa = dict

colecao = set, frontzenset

booleano = bool

binario = bytes, bytearray, memoryview